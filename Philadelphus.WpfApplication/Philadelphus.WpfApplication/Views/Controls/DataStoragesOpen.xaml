<UserControl x:Class="Philadelphus.WpfApplication.Views.Controls.DataStoragesOpen"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Philadelphus.WpfApplication.Views.Controls"
             xmlns:converters="clr-namespace:Philadelphus.WpfApplication.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <converters:IsAvailableToColorConverter x:Key="isAvailableToColorConverter"/>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
        </Grid.RowDefinitions>
        <GroupBox Grid.Row="0" Header="Выбрать хранилище">
            <ListBox ItemsSource="{Binding DataStorageVMs}" SelectedItem="{Binding SelectedDataStorageVM}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <TextBlock>
                        <Run Text="Наименование: "></Run><Run Text="{Binding DataStorage.Name, Mode=OneWay}"></Run><Run Text=". "></Run>
                        <Run Text="Описание: "></Run><Run Text="{Binding DataStorage.Description, Mode=OneWay}"></Run><Run Text=". "></Run>
                        <Run Text="Guid: "></Run><Run Text="{Binding DataStorage.Guid, Mode=OneWay}"></Run><Run Text=". "></Run>
                        <Run Text="Доступность: "></Run><Run Text="{Binding IsAvailable, Mode=OneWay}" FontWeight="Bold" Foreground="{Binding IsAvailable, Mode=OneWay, Converter={StaticResource isAvailableToColorConverter}}"></Run><Run Text=". "></Run>
                        <Run Text="("></Run><Run Text="{Binding LastCheckTime, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"></Run><Run Text=")"></Run>
                            </TextBlock>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </GroupBox>
        <Expander Grid.Row="1" IsExpanded="False" Header="Добавить новое хранилище">

        </Expander>
        <Button Grid.Row="2" Height="30" Content="Подтвердить"></Button>
    </Grid>
</UserControl>
