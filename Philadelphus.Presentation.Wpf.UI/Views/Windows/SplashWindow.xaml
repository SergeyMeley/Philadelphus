<Window x:Class="Philadelphus.Presentation.Wpf.UI.Views.Windows.SplashWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None" 
        WindowStartupLocation="CenterScreen"
        AllowsTransparency="True" 
        Background="Transparent"
        Topmost="True" ShowInTaskbar="False"
        Width="400" Height="300" 
        ResizeMode="NoResize">

    <Border Background="#E0F7FA" CornerRadius="20" 
            Width="380" Height="280" Margin="10"
            BorderBrush="#00BCD4" BorderThickness="2">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Логотип/Заголовок -->
            <TextBlock Grid.Row="1" Text="Чубушник" FontSize="48" FontWeight="Bold" 
                       HorizontalAlignment="Center" VerticalAlignment="Center" 
                       Foreground="#00695C"/>

            <!-- Анимация загрузки -->
            <Ellipse x:Name="Spinner" Grid.Row="2" Width="80" Height="80" 
                     HorizontalAlignment="Center" VerticalAlignment="Center"
                     Margin="0,20" Fill="#26C6DA">
                <Ellipse.RenderTransform>
                    <RotateTransform x:Name="RotateTransform" CenterX="40" CenterY="40"/>
                </Ellipse.RenderTransform>
                <Ellipse.Effect>
                    <DropShadowEffect Color="#00ACC1" ShadowDepth="0" BlurRadius="15" Opacity="0.7"/>
                </Ellipse.Effect>
                <Ellipse.Triggers>
                    <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation 
                                    Storyboard.TargetName="RotateTransform"
                                    Storyboard.TargetProperty="Angle"
                                    From="0" To="360" Duration="0:0:1.5"
                                    RepeatBehavior="Forever"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </Ellipse.Triggers>
            </Ellipse>

            <!-- Текст прогресса с пульсацией -->
            <TextBlock Grid.Row="2" x:Name="LoadingText" Text="Загрузка..." 
                       FontSize="20" HorizontalAlignment="Center" 
                       VerticalAlignment="Bottom" Margin="0,0,0,20" 
                       Foreground="#00695C">
                <TextBlock.Triggers>
                    <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation 
                                    Storyboard.TargetProperty="(UIElement.Opacity)"
                                    From="0.4" To="1.0" Duration="0:0:1.2"
                                    RepeatBehavior="Forever" AutoReverse="True"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </TextBlock.Triggers>
            </TextBlock>
        </Grid>
    </Border>
</Window>