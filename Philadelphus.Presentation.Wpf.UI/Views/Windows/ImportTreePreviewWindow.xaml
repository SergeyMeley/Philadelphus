<Window x:Class="Philadelphus.Presentation.Wpf.UI.Views.Windows.ImportTreePreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:repo="clr-namespace:Philadelphus.Presentation.Wpf.UI.Views.Controls.RepositoryExplorer"
        mc:Ignorable="d"
        Title="Предпросмотр импорта дерева"
        Height="780"
        Width="1240"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock x:Name="TxtSummary"
                       Text="Предпросмотр импорта."
                       FontSize="16"
                       FontWeight="SemiBold"/>
            <TextBlock Text="Ниже показан готовый RepositoryExplorer на временной preview-модели. Изменения и команды сохранения отключены."
                       Foreground="DimGray"
                       Margin="0,6,0,0"/>
        </StackPanel>

        <repo:RepositoryExplorer x:Name="RepositoryExplorerPreview"
                                 Grid.Row="1"/>

        <Button Grid.Row="2"
                Content="Закрыть"
                Width="120"
                Height="36"
                HorizontalAlignment="Right"
                Margin="0,16,0,0"
                Click="BtnClose_Click"/>
    </Grid>
</Window>
