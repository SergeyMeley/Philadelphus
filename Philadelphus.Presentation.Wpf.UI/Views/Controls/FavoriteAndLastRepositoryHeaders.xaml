<UserControl x:Class="Philadelphus.Presentation.Wpf.UI.Views.Controls.FavoriteAndLastRepositoryHeaders"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Philadelphus.Presentation.Wpf.UI.Views.Controls"
             xmlns:lbs="clr-namespace:Philadelphus.Presentation.Wpf.UI.Views.Controls.CollectionControls.ListBoxes"
             xmlns:detail="clr-namespace:Philadelphus.Presentation.Wpf.UI.Views.Controls.DetailedInformationGrids"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="2*"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition Width="auto"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <CheckBox Grid.Row="0" Grid.Column="0"
                  Content="Отобразить скрытые"
                  IsChecked="{Binding RepositoryHeadersCollectionVM.ShowHiddenRepositoryHeaders, Mode=TwoWay}"
                  Margin="4,2,4,2"
                  HorizontalAlignment="Left"></CheckBox>
        <GroupBox Grid.Row="1" Header="Избранные">
            <lbs:RepositoryHeadersListBox 
                RepositoryHeadersItemsSource="{Binding RepositoryHeadersCollectionVM.FavoritePhiladelphusRepositoryHeadersVMs}"
                RepositoryHeadersSelectedItem="{Binding RepositoryHeadersCollectionVM.SelectedPhiladelphusRepositoryHeaderVM, Mode=TwoWay}">
            </lbs:RepositoryHeadersListBox>
        </GroupBox>
        <GroupBox Grid.Row="2" Header="Недавние">
            <lbs:RepositoryHeadersListBox 
                RepositoryHeadersItemsSource="{Binding RepositoryHeadersCollectionVM.LastPhiladelphusRepositoryHeadersVMs}"
                RepositoryHeadersSelectedItem="{Binding RepositoryHeadersCollectionVM.SelectedPhiladelphusRepositoryHeaderVM, Mode=TwoWay}">
            </lbs:RepositoryHeadersListBox>
        </GroupBox>
        <Expander Grid.Row="0" Grid.Column="1" Grid.RowSpan="3" IsExpanded="False" ExpandDirection="Right">
            <Expander.Header>
                <TextBlock Text="Детальная информация">
                    <TextBlock.LayoutTransform>
                        <RotateTransform Angle="-90"/>
                    </TextBlock.LayoutTransform>
                </TextBlock>
            </Expander.Header>
            <detail:UniversalDetails DataContext="{Binding RepositoryHeadersCollectionVM.SelectedPhiladelphusRepositoryHeaderVM}"></detail:UniversalDetails>
        </Expander>
        <Button Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="100" Content="Выбрать и начать работу!" Height="30" Command="{Binding OpenMainWindowWithHeaderCommand}" Margin="1"></Button>
    </Grid>
</UserControl>
