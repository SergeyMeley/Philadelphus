<UserControl x:Class="Philadelphus.WpfApplication.Views.Controls.RepositoryCreation"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Philadelphus.WpfApplication.Views.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"></RowDefinition>
            <RowDefinition Height="30"></RowDefinition>
            <RowDefinition Height="30"></RowDefinition>
            <RowDefinition Height="30"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="30"></RowDefinition>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Grid.Column="0" Text="Наименование" Margin="2"></TextBlock>
        <TextBox Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="2" Text="{Binding Name}" Margin="2"></TextBox>
        <TextBlock Grid.Row="1" Grid.Column="0" Text="Описание" Margin="2"></TextBlock>
        <TextBox Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2" Text="{Binding Description}" Margin="2"></TextBox>
        <TextBlock Grid.Row="2" Grid.Column="0" Text="UUID" Margin="2"></TextBlock>
        <TextBox Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="2" Text="{Binding Guid, Mode=OneWay}" IsReadOnly="True" Margin="2"></TextBox>
        <TextBlock Grid.Row="3" Grid.Column="0" Text="Хранилище данных" Margin="2"></TextBlock>
        <ComboBox Grid.Row="3" Grid.Column="1" ItemsSource="{Binding DataStoragesSettingsVM.DataStorages}" SelectedValue="{Binding DataStoragesSettingsVM.SelectedDataStorage}" Margin="2">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Name}"></TextBlock>
                        <TextBlock Text=". Доступность: "></TextBlock>
                        <TextBlock Text="{Binding IsAvailable}" FontWeight="Bold" Foreground="{Binding IsAvailable, Mode=OneWay, Converter={StaticResource isAvailableToColorConverter}}"></TextBlock>
                    </StackPanel>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>
        <Button Grid.Row="3" Grid.Column="2" Command="{Binding OpenDataStoragesSettingsWindowCommand}" Content="Открыть настройки хранилищ" Margin="2"></Button>
        <Button Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="3" Command="{Binding CreateAndSaveRepositoryCommand}" Content="Добавить и сохранить" Margin="2"></Button>
    </Grid>
</UserControl>
