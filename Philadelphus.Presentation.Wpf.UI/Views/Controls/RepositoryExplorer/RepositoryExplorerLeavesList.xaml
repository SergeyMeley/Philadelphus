<UserControl x:Class="Philadelphus.Presentation.Wpf.UI.Views.Controls.RepositoryExplorer.RepositoryExplorerLeavesList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Philadelphus.Presentation.Wpf.UI.Views.Controls.RepositoryExplorer"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="auto"></RowDefinition>
            </Grid.RowDefinitions>
            <DataGrid Name="SelectedElementLeavesDataGrid" 
                      Grid.Row="0"
                      Grid.Column="0"
                      Margin="1" 
                      MinHeight="50"
                      ItemsSource="{Binding SelectedRepositoryMember.ChildLeaves}" 
                      SelectionChanged="SelectedElementLeavesDataGrid_SelectionChanged"
                      AutoGenerateColumns="False"
                      EnableRowVirtualization="True"
                      EnableColumnVirtualization="True"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Auto">
                <DataGrid.Columns>
                    <DataGridTemplateColumn Width="7"
                                            MinWidth="7">
                        <DataGridTemplateColumn.Header>
                            <TextBlock Text="" ToolTip="Состояние" MinWidth="7" />
                        </DataGridTemplateColumn.Header>
                        <DataGridTemplateColumn.CellStyle>
                            <Style TargetType="DataGridCell">
                                <Setter Property="Background" Value="{Binding State, Converter={StaticResource stateToColorConverter}}" />
                                <Setter Property="ToolTip" Value="{Binding State, Converter={StaticResource enumDisplayAttributeConverter}, ConverterParameter=Description}" />
                            </Style>
                        </DataGridTemplateColumn.CellStyle>
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="№" Binding="{Binding Sequence}" Width="auto"></DataGridTextColumn>
                    <DataGridTextColumn Header="Наименование" Binding="{Binding Name}" Width="auto"></DataGridTextColumn>
                    <DataGridTextColumn Header="Код" IsReadOnly="True" Binding="{Binding Uuid}" Width="auto"></DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
                <TextBlock Text="{Binding SelectedIndex,ElementName=SelectedElementLeavesDataGrid,Converter={StaticResource selectedIndexConverter}}" FontSize="10"></TextBlock>
                <TextBlock Text=" / " FontSize="10"></TextBlock>
                <TextBlock Text="{Binding Items.Count,ElementName=SelectedElementLeavesDataGrid}" FontSize="10"></TextBlock>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
