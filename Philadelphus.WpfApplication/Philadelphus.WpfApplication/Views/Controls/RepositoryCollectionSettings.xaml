<UserControl x:Class="Philadelphus.WpfApplication.Views.Controls.RepositoryCollection"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Philadelphus.WpfApplication.Views.Controls"
             xmlns:ctrls="clr-namespace:Philadelphus.WpfApplication.Views.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <ListBox Grid.Row="0" ItemsSource="{Binding RepositoryExplorerViewModel}" DisplayMemberPath="CurrentTreeRepository">
            
        </ListBox>
        <!--<DataGrid Grid.Row="0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="№"></DataGridTextColumn>
                <DataGridTextColumn Header="Путь"></DataGridTextColumn>
                <DataGridTextColumn Header="Доступность"></DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>-->
        <ctrls:RepositoryDetails Grid.Row="1"></ctrls:RepositoryDetails>
    </Grid>
</UserControl>
