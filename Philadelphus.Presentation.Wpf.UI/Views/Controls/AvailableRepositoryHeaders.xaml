<UserControl x:Class="Philadelphus.Presentation.Wpf.UI.Views.Controls.AvailableRepositoryHeaders"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Philadelphus.Presentation.Wpf.UI.Views.Controls"
             xmlns:detail="clr-namespace:Philadelphus.Presentation.Wpf.UI.Views.Controls.DetailedInformationGrids"
             xmlns:lbs="clr-namespace:Philadelphus.Presentation.Wpf.UI.Views.Controls.CollectionControls.ListBoxes"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="3*"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
        </Grid.RowDefinitions>
        <GroupBox Grid.Row="1" Header="Репозитории">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition></RowDefinition>
                    <RowDefinition Height="auto"></RowDefinition>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition Width="auto"></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <lbs:AvailableRepositoriesListBox DataContext="{Binding RepositoryCollectionVM}" RepositoriesItemsSource="{Binding PhiladelphusRepositoriesVMs}" RepositoriesSelectedItem="{Binding CurrentRepositoryVM, Mode=TwoWay}"></lbs:AvailableRepositoriesListBox>
                    <Expander Grid.Row="0" Grid.Column="1" Grid.RowSpan="1" IsExpanded="False" ExpandDirection="Right">
                    <Expander.Header>
                        <TextBlock Text="Детальная информация">
                            <TextBlock.LayoutTransform>
                                <RotateTransform Angle="-90"/>
                            </TextBlock.LayoutTransform>
                        </TextBlock>
                    </Expander.Header>
                    <detail:UniversalDetails DataContext="{Binding RepositoryCollectionVM.CurrentRepositoryExplorerVM}" Width="400" MinWidth="400" MaxWidth="700"></detail:UniversalDetails>
                </Expander>

                <Button Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="100" Content="Открыть" Height="30" Command="{Binding OpenMainWindowCommand}" Margin="2"></Button>
            </Grid>
        </GroupBox>
    </Grid>
</UserControl>
