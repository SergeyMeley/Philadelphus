<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:other="clr-namespace:Philadelphus.Presentation.ViewModels.EntitiesVMs.OtherEntitiesVMs;assembly=Philadelphus.Presentation"
             x:Class="Philadelphus.Presentation.Avalonia.Views.Controls.RepositoryExplorer.RepositoryRelations">
    <Grid RowDefinitions="Auto,*">
        <Button Grid.Row="0" Margin="4" HorizontalAlignment="Left"
                Content="Вычислить связи" Command="{Binding CalculateCommand}" />
        <TreeView Grid.Row="1" ItemsSource="{Binding Roots}">
            <TreeView.DataTemplates>
                <TreeDataTemplate DataType="other:RepositoryRelationVM" ItemsSource="{Binding Children}">
                    <Grid ColumnDefinitions="Auto,*,Auto,Auto" Margin="2">
                        <TextBlock Grid.Column="0" Margin="2,0,8,0" Text="{Binding Group}" />
                        <Button Grid.Column="1" HorizontalAlignment="Left" Background="Transparent"
                                Content="{Binding Element}" Command="{Binding NavigateCommand}" />
                        <TextBlock Grid.Column="2" Margin="12,0" Text="{Binding RelationType}" />
                        <Button Grid.Column="3" Content="Раскрыть" Command="{Binding LoadChildrenCommand}" />
                    </Grid>
                </TreeDataTemplate>
            </TreeView.DataTemplates>
        </TreeView>
    </Grid>
</UserControl>
