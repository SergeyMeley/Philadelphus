<UserControl x:Class="Philadelphus.Presentation.Wpf.UI.Views.Controls.FormulaTestControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="900">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0"
                   Grid.Column="0"
                   Margin="0,0,8,8"
                   VerticalAlignment="Center"
                   Text="Формула"/>
        <TextBox Grid.Row="0"
                 Grid.Column="1"
                 Margin="0,0,8,8"
                 MinHeight="28"
                 VerticalContentAlignment="Center"
                 Text="{Binding FormulaText, UpdateSourceTrigger=PropertyChanged}"/>
        <Button Grid.Row="0"
                Grid.Column="2"
                Margin="0,0,0,8"
                MinWidth="120"
                Padding="12,4"
                Command="{Binding EvaluateFormulaCommand}"
                Content="Вычислить"/>

        <TextBlock Grid.Row="1"
                   Grid.Column="0"
                   Margin="0,0,8,8"
                   VerticalAlignment="Center"
                   Text="Результат"/>
        <TextBox Grid.Row="1"
                 Grid.Column="1"
                 Grid.ColumnSpan="2"
                 Margin="0,0,0,8"
                 MinHeight="28"
                 IsReadOnly="True"
                 VerticalContentAlignment="Center"
                 Text="{Binding ResultText, Mode=OneWay}"/>

        <TextBlock Grid.Row="2"
                   Grid.Column="0"
                   Margin="0,0,8,8"
                   VerticalAlignment="Center"
                   Text="Тип"/>
        <TextBox Grid.Row="2"
                 Grid.Column="1"
                 Grid.ColumnSpan="2"
                 Margin="0,0,0,8"
                 MinHeight="28"
                 IsReadOnly="True"
                 VerticalContentAlignment="Center"
                 Text="{Binding ResultTypeText, Mode=OneWay}"/>

        <TextBlock Grid.Row="3"
                   Grid.Column="0"
                   Margin="0,0,8,0"
                   VerticalAlignment="Top"
                   Text="Ошибка"/>
        <TextBox Grid.Row="3"
                 Grid.Column="1"
                 Grid.ColumnSpan="2"
                 MinHeight="80"
                 AcceptsReturn="True"
                 IsReadOnly="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 Text="{Binding ErrorText, Mode=OneWay}"/>
    </Grid>
</UserControl>
