<Window x:Class="Philadelphus.Presentation.Wpf.UI.Views.Windows.LaunchWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Philadelphus.Presentation.Wpf.UI.Views.Windows"
        xmlns:lbs="clr-namespace:Philadelphus.Presentation.Wpf.UI.Views.Controls.CollectionControls.ListBoxes"
        xmlns:ctrls="clr-namespace:Philadelphus.Presentation.Wpf.UI.Views.Controls"
        xmlns:detail="clr-namespace:Philadelphus.Presentation.Wpf.UI.Views.Controls.DetailedInformationGrids"
        xmlns:converters="clr-namespace:Philadelphus.Presentation.Wpf.UI.Converters"
        mc:Ignorable="d"
        Title="Выбор репозитория"
        MinHeight="500" MinWidth="1000"
        WindowState="Maximized">
    <Window.Visibility>
        <Binding Path="StartWindowVisibility"/>
    </Window.Visibility>
    <Window.Resources>
        <converters:IsAvailableToColorConverter x:Key="isAvailableToColorConverter"/>

        <SolidColorBrush x:Key="TabControlNormalBorderBrush" Color="#8C8E94"/>
        <Style x:Key="LeftVerticalTabControlStyle" TargetType="{x:Type TabControl}">
            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
            <Setter Property="Padding" Value="4,4,4,4"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{StaticResource TabControlNormalBorderBrush}"/>
            <Setter Property="Background" Value="#F9F9F9"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabControl">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" 
                                        Orientation="Vertical" 
                                        IsItemsHost="True"/>
                            <Border Grid.Column="1" 
                                    x:Name="ContentPanel" 
                                    BorderBrush="{TemplateBinding BorderBrush}" 
                                    BorderThickness="{TemplateBinding BorderThickness}" 
                                    Background="{TemplateBinding Background}" 
                                    KeyboardNavigation.DirectionalNavigation="Contained" 
                                    KeyboardNavigation.TabIndex="2" 
                                    KeyboardNavigation.TabNavigation="Local">
                                <ContentPresenter x:Name="PART_SelectedContentHost" 
                                                  ContentSource="SelectedContent" 
                                                  Margin="{TemplateBinding Padding}" 
                                                  SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="LeftVerticalTabItemStyle" TargetType="TabItem">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                            <Border Name="Border" BorderThickness="1,1,0,1" BorderBrush="LightSlateGray" Margin="1">
                                <StackPanel Orientation="Vertical" 
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            Height="75"
                                            Width="90"
                                            Margin="5,5">
                                    <ContentControl Content="{TemplateBinding Tag}"
                                           Width="45" Height="45" 
                                           Margin="0,5,0,0"/>
                                    <TextBlock Text="{TemplateBinding Header}" 
                                               HorizontalAlignment="Center"
                                               Margin="0,5,0,5"
                                               FontSize="14"
                                               TextWrapping="Wrap"
                                               TextAlignment="Center"/>
                                </StackPanel>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="LightSkyBlue" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid>
        <!--<TabControl Style="{StaticResource LeftVerticalTabControlStyle}">

            <TabItem Header="Хранилища" Style="{StaticResource LeftVerticalTabItemStyle}" Tag="{DynamicResource imageStorage}">
                <ctrls:DataStorageHeaders></ctrls:DataStorageHeaders>
            </TabItem>

            <TabItem Header="Настройки" Style="{StaticResource LeftVerticalTabItemStyle}" Tag="{DynamicResource imageSettings}">
                <ctrls:ApplicationSettings DataContext="{Binding ApplicationSettingsControlVM}"></ctrls:ApplicationSettings>
            </TabItem>
        </TabControl>-->
        <TabControl Style="{StaticResource LeftVerticalTabControlStyle}"
            ItemsSource="{Binding LaunchWindowTabItemsVMs}"
            SelectedItem="{Binding SelectedLaunchWindowTabItemVM, Mode=TwoWay}">
            <TabControl.ItemTemplate>
                <DataTemplate>
                    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Border Name="Border" BorderThickness="1,1,0,1" BorderBrush="LightSlateGray" Margin="1">
                            <StackPanel Orientation="Vertical" HorizontalAlignment="Center" 
                                VerticalAlignment="Center" Height="75" Width="90" Margin="5,5">
                                <!--<ContentControl Content="{Binding IconKey, Converter={StaticResource IconConverter}}"
                                       Width="45" Height="45" Margin="0,5,0,0"/>-->
                                <TextBlock Text="{Binding Header}" HorizontalAlignment="Center" 
                                   Margin="0,5,0,5" FontSize="14" TextWrapping="Wrap"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                </DataTemplate>
            </TabControl.ItemTemplate>
            <TabControl.ContentTemplate>
                <DataTemplate>
                    <ContentControl Content="{Binding Content}"/>
                </DataTemplate>
            </TabControl.ContentTemplate>
        </TabControl>

    </Grid>
</Window>