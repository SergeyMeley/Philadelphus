<UserControl x:Class="Philadelphus.WpfApplication.Views.Controls.OwnAttributes"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Philadelphus.WpfApplication.Views.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel>
            <DataGrid Height="auto" ItemsSource="{Binding SelectedRepositoryMember.ParentElementAttributesVMs}" AutoGenerateColumns="false">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Наименование" Binding="{Binding Name}"></DataGridTextColumn>
                    <DataGridComboBoxColumn Header="Тип данных"></DataGridComboBoxColumn>
                    <DataGridCheckBoxColumn Header="Может быть пустым"></DataGridCheckBoxColumn>
                    <DataGridComboBoxColumn Header="Область видимости" ItemsSource="{Binding VisabilityList}"></DataGridComboBoxColumn>
                    <DataGridTextColumn Header="Источник" Binding="{Binding Owner.Name}"></DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </Grid>
</UserControl>
