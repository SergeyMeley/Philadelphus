<UserControl x:Class="Philadelphus.Presentation.Wpf.UI.Views.Controls.ApplicationSettings"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Philadelphus.Presentation.Wpf.UI.Views.Controls"
             xmlns:settings="clr-namespace:Philadelphus.Presentation.Wpf.UI.Views.Controls.Settings"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <SolidColorBrush x:Key="TabControlNormalBorderBrush" Color="#8C8E94"/>
        <Style x:Key="LeftVerticalTabControlStyle" TargetType="{x:Type TabControl}">
            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
            <Setter Property="Padding" Value="4,4,4,4"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{StaticResource TabControlNormalBorderBrush}"/>
            <Setter Property="Background" Value="#F9F9F9"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabControl">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" 
                                        Orientation="Vertical" 
                                        IsItemsHost="True"/>
                            <Border Grid.Column="1" 
                                    x:Name="ContentPanel" 
                                    BorderBrush="{TemplateBinding BorderBrush}" 
                                    BorderThickness="{TemplateBinding BorderThickness}" 
                                    Background="{TemplateBinding Background}" 
                                    KeyboardNavigation.DirectionalNavigation="Contained" 
                                    KeyboardNavigation.TabIndex="2" 
                                    KeyboardNavigation.TabNavigation="Local">
                                <ContentPresenter x:Name="PART_SelectedContentHost" 
                                                  ContentSource="SelectedContent" 
                                                  Margin="{TemplateBinding Padding}" 
                                                  SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="LeftVerticalTabItemStyle" TargetType="TabItem">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                            <Border Name="Border" BorderThickness="1,1,0,1" BorderBrush="LightSlateGray" Margin="1">
                                <StackPanel Orientation="Vertical" 
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            MinHeight="20"
                                            Width="150"
                                            Margin="5,5">
                                    <TextBlock Text="{TemplateBinding Header}" 
                                               HorizontalAlignment="Center"
                                               Margin="0,5,0,5"
                                               FontSize="14"
                                               TextWrapping="Wrap"
                                               TextAlignment="Center"/>
                                </StackPanel>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="LightSkyBlue" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid>
        <TabControl Style="{StaticResource LeftVerticalTabControlStyle}">
            ы
            <TabItem Header="Настроечные файлы" Style="{StaticResource LeftVerticalTabItemStyle}">
                <settings:ConfigurationFilesSettings></settings:ConfigurationFilesSettings>
            </TabItem>
            <TabItem Header="Строки подключения" Style="{StaticResource LeftVerticalTabItemStyle}">
                <settings:ConnectionStringContainersSettings></settings:ConnectionStringContainersSettings>
            </TabItem>
            <!--<TabItem Header="Конфиденциальная информация" Style="{StaticResource LeftVerticalTabItemStyle}">
                <TextBlock Text="Здесь пока ничего нет, но мы работаем над этим ;-)" TextWrapping="Wrap" TextAlignment="Center" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="40" Margin="100"></TextBlock>
            </TabItem>
            <TabItem Header="Учетная запись" Style="{StaticResource LeftVerticalTabItemStyle}">
                <TextBlock Text="Здесь пока ничего нет, но мы работаем над этим ;-)" TextWrapping="Wrap" TextAlignment="Center" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="40" Margin="100"></TextBlock>
            </TabItem>
            <TabItem Header="Сочетания клавиш" Style="{StaticResource LeftVerticalTabItemStyle}">
                <TextBlock Text="Здесь пока ничего нет, но мы работаем над этим ;-)" TextWrapping="Wrap" TextAlignment="Center" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="40" Margin="100"></TextBlock>
            </TabItem>-->
        </TabControl>

    </Grid>
</UserControl>
