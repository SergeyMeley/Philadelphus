<UserControl x:Class="Philadelphus.WpfApplication.Views.Controls.RepositoryCollectionSettings"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Philadelphus.WpfApplication.Views.Controls"
             xmlns:ctrls="clr-namespace:Philadelphus.WpfApplication.Views.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="3*"></RowDefinition>
        </Grid.RowDefinitions>
        <ListBox Grid.Row="0" ItemsSource="{Binding TreeRepositoriesVMs}" DisplayMemberPath="TreeRepository.Name" SelectedItem="{Binding CurrentRepositoryExplorerVM}" SelectedValuePath="TreeRepository.Name">

        </ListBox>
        <!--<DataGrid Grid.Row="0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="№"></DataGridTextColumn>
                <DataGridTextColumn Header="Путь"></DataGridTextColumn>
                <DataGridTextColumn Header="Доступность"></DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>-->
        <GridSplitter Grid.Row="1" Grid.Column="0" ShowsPreview="False" Height="3" HorizontalAlignment="Stretch" VerticalAlignment="Center" Grid.ColumnSpan="2"></GridSplitter>
        <ctrls:RepositoryDetails Grid.Row="2"></ctrls:RepositoryDetails>
    </Grid>
</UserControl>
